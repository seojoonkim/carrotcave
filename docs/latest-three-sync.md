# Latest-three Telegram sync

Repository: `seojoonkim/carrotcave`; canonical: https://carrotcave.com; Vercel linked project: `carrotcave`. Run from this repository with `HOME=/Users/gimseojun`. Cron ownership stays outside this procedure.

## Minimal path

1. Inspect `git status --short` and the pending diff first. Preserve authorized posts/state/overrides/media/ontology work from earlier attempts. Read https://t.me/s/carrotcave and choose the latest **three text-bearing editorial messages**, not three consecutive numbers (this release: **213, 211, 210**; 212 is not an editorial entry in the public feed).
2. Compare each source title, entire body, Seoul publication date and media against `data/posts.ts` plus `data/sync-state.json`. For already matching entries, do not republish. `node scripts/verify-latest-three.mjs` records exact source/local matches in `docs/eval/latest-three-source.json`. The timestamp selector must be `time[datetime]`, since video duration also uses a `time` element.
3. For a missing entry, review its metadata in `data/sync-metadata-overrides.json`, then run the existing bounded `npm run publish:telegram -- ID`. Read that script's current options first. Never use the full historical sync merely to publish the latest three. Preserve the verbatim source; do not invent title, date or body.
4. If pending ontology artifacts lack the semantic sample, regenerate deterministically: `node scripts/draft-ontology.mjs && node scripts/build-ontology-index.mjs`. This updates `docs/eval/ontology-semantic-sample.json` along with ontology data. Then run **`npm run verify` once** for the reviewed artifact. It includes all tests, title/abstract/ontology audits and production build. Inspect failures; never delete tests or bypass audits.
5. Smoke-test the production build with `npm run start -- --hostname 127.0.0.1 --port 3187` and `APP_URL=http://127.0.0.1:3187 PROOF_PATH=docs/eval/latest-three-local.json node scripts/verify-latest-three.mjs`. This checks source equality, all three readers, home-card navigation, mobile/desktop overflow, prose, source links, loaded images, asset hashes, footer identity and browser errors.
6. Review and explicitly stage only the content, associated media/ontology, narrow test changes and release documentation/proof. Commit, push main, verify remote HEAD. Inspect GitHub-connected Vercel deployment first; if none was created, use `HOME=/Users/gimseojun vercel --prod --yes`. Confirm Ready and canonical alias before live readback.
7. Run `APP_URL=https://carrotcave.com PROOF_PATH=docs/eval/latest-three-live.json node scripts/verify-latest-three.mjs`. Record deployment URL/ID, release commit, verification totals and source/proof hashes in `docs/eval/latest-three-release.json`. Persist proof in a follow-up documentation commit. Report exact three IDs/title/slug/status and canonical proof; a local build or temporary test is not publication.

## Why the previous attempts stopped

Baseline at `dd5c10f`: 188/193 tests passed; five legacy assertions contradicted intentional UI changes. Commit `3b355ec` removed the home ENTRIES/SECTION chrome and segmented date boxes, while retaining accessible dates. `dd5c10f` restored animated `footer-rabbit-carrot-v2.svg` and removed the static CSS override. Stale assertions lived in `archive-redesign.test.mjs` (two), `content-contract.test.mjs`, `editorial-card-render.test.mjs`, and `footer-header-contract.test.mjs`. This release updates only those assertions to the current intentional behavior. Existing animation/timeline/reduced-motion contracts remain enabled. No UI or SVG changes belong in routine content sync.
